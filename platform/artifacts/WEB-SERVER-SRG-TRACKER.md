# Web Server SRG — implementation tracker

`U_Web_Server_SRG_V4R5_Manual-xccdf.xml` · 126 controls · 6 CAT I (high), 120 CAT II (medium)

Generated by [`gen_srg_tracker.py`](gen_srg_tracker.py) from the benchmark plus
[`srg_status.yml`](srg_status.yml), which holds the determinations. Edit the status file,
never this one; `--check` fails when this file is stale or a control is unassessed.

The web tier this applies to: **Traefik** (ingress, TLS termination), **oauth2-proxy**
(the SSO gate), **nginx** (serving the React application) and the **Django REST API**
behind it, with **Keycloak** as the identity provider.

## Where it stands

| Status | Count | Meaning |
|---|---:|---|
| Mitigated | 105 | Satisfied by a mechanism the platform already has |
| Implement | 12 | Feasible on the current architecture |
| Redesign | 9 | Needs an architectural or web-tier redesign |
| Not applicable | 0 | Does not apply, with justification |
| Open | 0 | Not yet assessed |

Of the 105 mitigated, **83 are verified by a test** and 22 rest on inspection alone. A mitigation nobody checked is a claim; closing that gap is part of the work, not a footnote to it.

### CAT I

| Control | Status | Title |
|---|---|---|
| `SRG-APP-000172-WSR-000104` | Mitigated | The web server must encrypt passwords during transmission. |
| `SRG-APP-000179-WSR-000110` | Redesign | The web server must use cryptographic modules that meet the requirements of applicable federal laws, Executive Orders, directives, policies, regulations, standards, and guidance when encrypting stored data. |
| `SRG-APP-000224-WSR-000135` | Redesign | The web server must generate a unique session identifier for each session using a FIPS 140-2 approved random number generator. |
| `SRG-APP-000429-WSR-000113` | Mitigated | The web server must encrypt user identifiers and passwords. |
| `SRG-APP-000439-WSR-000151` | Mitigated | The web server must employ cryptographic mechanisms (TLS/DTLS/SSL) preventing the unauthorized disclosure of information during transmission. |
| `SRG-APP-001035-WSR-000340` | Mitigated | The web server must be a version supported by the vendor. |

## Redesign — 9

| Control | V-ID | Sev | Title | What it takes | Where |
|---|---|---|---|---|---|
| `SRG-APP-000175-WSR-000095` | V-206388 | MED | The web server must perform RFC 5280-compliant certification path validation. | RFC 5280 certification path validation implies a real PKI. The platform pins self-signed CAs deliberately (the enclave has no egress, so no CRL/OCSP responder is reachable), which satisfies the trust decision but not path validation. Meeting this means issuing web-tier certificates from DoD PKI and providing revocation data inside the enclave. | PKI track — traefik/gen-cert.sh, hashicorp/access/gen-boundary-cert.sh |
| `SRG-APP-000179-WSR-000110` | V-206390 | HIG | The web server must use cryptographic modules that meet the requirements of applicable federal laws, Executive Orders, directives, policies, regulations, standards, and guidance when encrypting stored data. | FIPS 140-2/140-3 validated modules. The images use their distributions' stock OpenSSL/Go crypto, which is not a validated module — this needs FIPS-mode base images and a validated provider across Traefik, oauth2-proxy, nginx, Keycloak and Python. It is a rebuild of the web tier's runtime, not a setting. | FIPS track — every web-tier Dockerfile and base image |
| `SRG-APP-000179-WSR-000111` | V-206391 | MED | The web server must use cryptographic modules that meet the requirements of applicable federal laws, Executive Orders, directives, policies, regulations, standards, and guidance for such authentication. | Same FIPS module requirement, stated for the hosted application's crypto. | FIPS track |
| `SRG-APP-000224-WSR-000135` | V-206399 | HIG | The web server must generate a unique session identifier for each session using a FIPS 140-2 approved random number generator. | The identifier is unique per session, but "FIPS 140-2 approved random number generator" is a claim about the crypto module, not the application. Ties to the FIPS track below — it cannot be closed by configuring oauth2-proxy or Keycloak. | FIPS track (SRG-APP-000179-*) |
| `SRG-APP-000225-WSR-000141` | V-206406 | MED | The web server must provide a clustering capability. | No clustering. Postgres, MinIO, Redis and the ingress are single instances; the platform is designed for one enclave. This is the horizontal-scalability track, and it is a genuine architecture change rather than a setting. | planning/enterprise_storage_roadmap.md, planning/PARALLEL-ANALYSIS-CAPACITY.md |
| `SRG-APP-000231-WSR-000144` | V-206407 | MED | Information at rest must be encrypted using a DOD-accepted algorithm to protect the confidentiality and integrity of the information. | Data at rest is not encrypted: MinIO stores captures unencrypted and Postgres has no transparent encryption. This is the largest remaining gap for a platform holding memory images — it needs MinIO server-side encryption with a Vault-held key, plus an encrypted volume or pgcrypto decision for the metadata store. | deploy/enclave/docker-compose.yml (MinIO KMS), hashicorp/vault/ |
| `SRG-APP-000416-WSR-000118` | V-239371 | MED | The web server must implement required cryptographic protections using cryptographic modules complying with applicable federal laws, Executive Orders, directives, policies, regulations, standards, and guidance when encrypting data that must be compartmentalized. | Same FIPS module requirement, stated as "cryptographic modules complying with applicable laws". | FIPS track |
| `SRG-APP-000439-WSR-000193` | V-264363 | MED | The web server must disable HTTP/1.x downgrading. | Not reachable with this ingress. Refusing HTTP/1.x means not OFFERING http/1.1 in ALPN, and Traefik exposes no control over the ALPN list — its http2 settings tune streams and header tables only. Meeting this needs an ingress that can restrict ALPN, which is an ingress replacement rather than a setting. Weigh that against the exposure: HTTP/2 is negotiated and used, the only client is the workstation's managed browser, and the downgrade path is a fallback nothing on the analyst path takes. | traefik/ — would require a different ingress, or an ALPN control Traefik does not have |
| `SRG-APP-000875-WSR-000280` | V-264354 | MED | The web server must, for public key-based authentication, implement a local cache of revocation data to support path discovery and validation. | A local cache of revocation data presumes revocation data exists (see WSR-000095). Inside an enclave with no egress it must be staged in like symbol tables are. | PKI track |

## Implement — 12

| Control | V-ID | Sev | Title | What it takes | Where |
|---|---|---|---|---|---|
| `SRG-APP-000016-WSR-000005` | V-206354 | MED | The web server must generate information to be used by external applications or entities to monitor and control remote access. | Aggregated into a PRIVATE MinIO bucket (ir-logs), held apart from evidence and carved regions and reachable only through the mesh. Object storage is the 'different system or media' the control asks for without adding an egress path or a second product to run: the store already exists, is already backed up as a unit, and already has a retention lifecycle the platform understands. An external monitor reads the bucket rather than being given a route to the ingress. | deploy/enclave/docker-compose.yml (log-shipper), backend/cases/storage.py (ir-logs bucket), backend/cases/componenthealth.py |
| `SRG-APP-000219-WSR-000190` | V-264360 | MED | The web server must restrict a consistent inbound source IP for the entire management session. | Management sessions are not pinned to their originating address. Boundary already records the client address per session, so the enforcement point exists; binding the gate's session to it is the work. | deploy/enclave/docker-compose.yml, backend/cases/authentication.py |
| `SRG-APP-000219-WSR-000191` | V-264361 | MED | The web server must restrict a consistent inbound source IP for the entire user session. | Same for user sessions. Note the interaction with the brokered path — every analyst arrives from the egress worker's address, so the pin must be applied at the broker, not the gate, or every session shares one source. | hashicorp/access/boundary_session.sh, deploy/enclave/docker-compose.yml |
| `SRG-APP-000427-WSR-000186` | V-206430 | MED | The web server must only accept client certificates (user and machine) issued by DOD PKI or DOD-approved PKI Certificate Authorities (CAs). | The PLUMBING is built and inert: hashicorp/keycloak/pki-logon/ holds the x509 browser flow, a trust-anchor allow-list and a CRL directory, and deploy.sh renders both the ingress client-auth block and the realm flow from IR_PKI_LOGON — rendering the DISABLED form explicitly, so a deployment that once had it on stops asking for certificates. Enabling it with no trust anchor is refused, because that is a lockout rather than a hardening step. What remains is not code: an issuing CA, cards carrying its certificates, and revocation data staged into the enclave. It stays opt-in until an environment has them. | hashicorp/keycloak/pki-logon/, deploy/deploy.sh (pki_logon_render), traefik/dynamic- sso/dynamic.yml, deploy/.env (IR_PKI_LOGON) |
| `SRG-APP-000516-WSR-000174` | V-206445 | MED | The web server must be configured in accordance with the security configuration settings based on DoD security configuration or implementation guidance, including STIGs, NSA configuration guides, CTOs, and DTMs. | This tracker is the response to that requirement. It closes when the controls above are resolved and the result is recorded against each one. | artifacts/WEB-SERVER-SRG-TRACKER.md |
| `SRG-APP-000700-WSR-000100` | V-264337 | MED | The web server must disable accounts when the accounts have expired. | Account expiry is not configured. Keycloak supports an account expiry attribute and disabled state; the platform's user admin must surface and honour it. | keycloak/realm-irplatform.json, backend/cases/keycloak_admin.py |
| `SRG-APP-000705-WSR-000110` | V-264338 | MED | The web server must disable accounts when the accounts are no longer associated to a user. | Disabling accounts no longer associated with a user is the same lifecycle work as WSR-000100, plus an operational review procedure. | backend/cases/keycloak_admin.py |
| `SRG-APP-000745-WSR-000120` | V-264339 | MED | The web server must implement the capability to centrally review and analyze audit records from multiple components within the system. | Aggregated into a PRIVATE MinIO bucket (ir-logs), held apart from evidence and carved regions and reachable only through the mesh. Object storage is the 'different system or media' the control asks for without adding an egress path or a second product to run: the store already exists, is already backed up as a unit, and already has a retention lifecycle the platform understands. One place to review web-tier records, beside the application's own audit trail. | deploy/enclave/docker-compose.yml (log-shipper), backend/cases/storage.py (ir-logs bucket), backend/cases/componenthealth.py |
| `SRG-APP-000795-WSR-000130` | V-264340 | MED | The web server must alert organization-defined personnel or roles upon detection of unauthorized access, modification, or deletion of audit information. | Alerting on detected unauthorized activity — no alert path exists; the Component Health reporter is again the carrier. | backend/cases/healthreporter.py |
| `SRG-APP-000805-WSR-000140` | V-264341 | MED | The web server must automatically generate audit records of the enforcement actions. | Enforcement actions are partly recorded — RBAC denials return 403 but are not written to the ledger. Recording denials is a bounded backend change. | backend/cases/rbac.py, backend/cases/audit.py |
| `SRG-APP-000820-WSR-000170` | V-264343 | MED | The web server must implement multifactor authentication for local; network; and/or remote access to privileged accounts; and/or nonprivileged accounts such that one of the factors is provided by a device separate from the system gaining access. | MFA is not configured. Keycloak supports OTP as a required action and conditional flow; enabling it for the admin and analyst groups is configuration in the realm, not a redesign. Hardware-token (CAC/PIV) MFA is the PKI track instead. | keycloak/realm-irplatform.json (browser flow, OTP required action) |
| `SRG-APP-000825-WSR-000180` | V-264344 | MED | The web server must implement multifactor authentication for local; network; and/or remote access to privileged accounts; and/or nonprivileged accounts such that the device meets organization-defined strength of mechanism requirements. | Same realm change, stated for non-privileged accounts. | keycloak/realm-irplatform.json |

## Mitigated — 105

| Control | V-ID | Sev | Title | Mechanism | Evidence | Verified by |
|---|---|---|---|---|---|---|
| `SRG-APP-000001-WSR-000001` | V-206350 | MED | The web server must limit the number of allowed simultaneous session requests. | Three ceilings, all bounded rather than generous: the ingress bounds simultaneous in-flight requests and HTTP/2 streams, nginx bounds connections per source address, and request HEADER capacity is bounded by capping per-request CSRF cookies at the SSO gate instead of enlarging the server's header buffers. Header buffers are per-connection memory and every analyst shares one source address through the broker, so sizing them to absorb unbounded cookie accumulation would have weakened the per-source bound to hide a fixable defect. | traefik/dynamic-sso/dynamic.yml (inflight), traefik/traefik.yml (http2.maxConcurrentStreams), frontend/nginx.conf.template (limit_conn, large_client_header_buffers 4 16k), deploy/enclave/docker-compose.yml (--cookie-csrf-per-request-limit) | test/uat_srg_webtier.sh — connection and stream limits asserted as deployed; the CSRF cookie cap is asserted at the gate and the header buffer asserted to remain at its bounded value, so a regression that widens it to absorb accumulation fails the test |
| `SRG-APP-000001-WSR-000002` | V-206351 | MED | The web server must perform server-side session management. | Sessions are held server-side in Redis, not in the client's cookie, so signing out or canceling a session ends it for real. The gate is a mesh service in its own right: Redis binds loopback only, so the gate reaches it through its own Connect sidecar and its access is an intention rather than network reachability. Database 1 keeps analyst sessions out of the task broker's keyspace. | deploy/enclave/docker-compose.yml (oauth2-proxy --session-store-type=redis, oauth2-proxy- sidecar), hashicorp/consul/config-entries/ir-redis.hcl, hashicorp/consul/register-mesh.sh | test/uat_srg_webtier.sh — a real authorization-code login through the ingress grows the session database from 0 to 1 key; with a cookie store that count never moves |
| `SRG-APP-000014-WSR-000006` | V-206352 | MED | The web server must use encryption strength in accordance with the categorization of data hosted by the web server when remote connections are provided. | TLS 1.2 floor with an explicit AEAD, forward-secret cipher list and sniStrict, stated in the ingress rather than left to the library default. | traefik/dynamic-sso/dynamic.yml (tls.options.default) | test/uat_srg_webtier.sh — a default client negotiates TLSv1.3 with an AEAD suite; TLS 1.1 is refused at the handshake |
| `SRG-APP-000015-WSR-000014` | V-206353 | MED | The web server must use cryptography to protect the integrity of remote sessions. | Every remote session is a Boundary session carried over per-session mutual TLS, reaching an ingress that serves TLS only. The analyst has no unencrypted path to reach. | SECURITY-MODEL.md P4; hashicorp/access/boundary_session.sh refuses a non-https controller address | test/uat_boundary.sh — the client address is https, the certificate is pinned, and the controller API does not answer plaintext |
| `SRG-APP-000033-WSR-000169` | V-206355 | MED | The web server must enforce approved authorizations for logical access to hosted applications and resources in accordance with applicable access control policies. | Every request passes the SSO gate; Keycloak group membership maps to a platform role, and the API enforces per-role permissions. The backend trusts forwarded identity only when the proxy shared secret matches, so headers cannot be spoofed by a direct client. | backend/cases/authentication.py, backend/cases/rbac.py | test/uat_baseline.sh; test/uat_e2e.sh (RBAC section — an analyst cannot delete) |
| `SRG-APP-000089-WSR-000047` | V-206356 | MED | The web server must generate, at a minimum, log records for system startup and shutdown, system access, and system authentication events. | Structured JSON access logging is enabled explicitly on the ingress. | traefik/traefik.yml (accessLog) | test/uat_srg_webtier.sh — a structured record is present for the request the test made |
| `SRG-APP-000092-WSR-000055` | V-206357 | MED | The web server must initiate session logging upon start up. | Access logging is part of the ingress configuration, so it begins with the process rather than being switched on afterwards. | traefik/traefik.yml (accessLog) | test/uat_srg_webtier.sh — records exist for this run's requests |
| `SRG-APP-000095-WSR-000056` | V-206359 | MED | The web server must produce log records containing sufficient information to establish what type of events occurred. | The access-log record names the request method. | traefik/traefik.yml (accessLog.fields) | test/uat_srg_webtier.sh — RequestMethod is present |
| `SRG-APP-000096-WSR-000057` | V-206360 | MED | The web server must produce log records containing sufficient information to establish when (date and time) events occurred. | The application's audit ledger timestamps every entry, and container logs carry timestamps; Traefik's access log records request time. | backend/cases/audit.py | test/uat_e2e.sh — the audit trail section |
| `SRG-APP-000097-WSR-000058` | V-206361 | MED | The web server must produce log records containing sufficient information to establish where within the web server the events occurred. | The record names the router and service the request reached. | traefik/traefik.yml (accessLog.fields) | test/uat_srg_webtier.sh — RouterName is present |
| `SRG-APP-000098-WSR-000059` | V-206362 | MED | The web server must produce log records containing sufficient information to establish the source of events. | The record names the source address of the connection. | traefik/traefik.yml (accessLog.fields) | test/uat_srg_webtier.sh — ClientHost is present |
| `SRG-APP-000098-WSR-000060` | V-206363 | MED | A web server, behind a load balancer or proxy server, must produce log records containing the client IP information as the source and destination and not the load balancer or proxy IP information with each event. | Attribution behind the proxy is a JOIN, not a header. Boundary's hop is a TCP session, so no HTTP proxy exists to insert X-Forwarded-For and ClientHost is the egress worker for every analyst. The ingress record carries StartUTC and ClientHost; the broker's session record carries the real client address for that window, and the two join on time and session. The ingress also captures X-Forwarded-For when present, so a future HTTP proxy in front would be recorded directly. | traefik/traefik.yml (accessLog.fields.headers), backend/cases/brokeredsessions.py | test/uat_srg_webtier.sh — the test drives a request through the brokered listener, then asserts the ingress record carries the join keys and the session reports the real client address |
| `SRG-APP-000099-WSR-000061` | V-206364 | MED | The web server must produce log records that contain sufficient information to establish the outcome (success or failure) of events. | The record carries the response status. | traefik/traefik.yml (accessLog.fields) | test/uat_srg_webtier.sh — DownstreamStatus is present |
| `SRG-APP-000100-WSR-000064` | V-206365 | MED | The web server must produce log records containing sufficient information to establish the identity of any user/subject or process associated with an event. | The application's audit trail records the acting identity for every mutation, taken from the SSO assertion rather than from a client-supplied header. | backend/cases/audit.py, backend/cases/authentication.py | test/uat_e2e.sh |
| `SRG-APP-000108-WSR-000166` | V-206366 | MED | The web server must use a logging mechanism that is configured to alert the ISSO and SA in the event of a processing failure. | The log shipper self-reports to Component Health; a pass failure rides the next report as an alert, and a shipper that goes quiet is surfaced as stale rather than assumed healthy. | backend/cases/management/commands/ship_logs.py (healthreporter wiring), backend/cases/componenthealth.py (log alerts, stale rows) | test/uat_srg_webtier.sh — the shipper's health row exists and a reported failure raises an alert |
| `SRG-APP-000116-WSR-000066` | V-206367 | MED | The web server must use the internal system clock to generate time stamps for log records. | Timestamps come from the system clock in every component; nothing accepts a client-supplied time. | backend/cases/audit.py | **not verified** |
| `SRG-APP-000118-WSR-000068` | V-206368 | MED | Web server log files must only be accessible by privileged users. | The audit trail is readable only by auditors and admins; container logs are reachable only with runtime access, which the web tier does not have. | backend/cases/rbac.py; SECURITY-MODEL.md P10 | test/uat_e2e.sh — the auditor reads the trail, the analyst cannot |
| `SRG-APP-000119-WSR-000069` | V-206369 | MED | The log information from the web server must be protected from unauthorized modification. | The audit ledger is append-only and hash-chained; modification breaks the chain and the verification says where. Nothing in the API can rewrite an entry. | backend/cases/audit.py | test/uat_e2e.sh — the hash chain verifies over the whole ledger |
| `SRG-APP-000120-WSR-000070` | V-206370 | MED | The log information from the web server must be protected from unauthorized deletion. | Deletion is equally detectable — the chain is verified over the whole ledger, not the visible page, so a removed entry breaks it. | backend/cases/audit.py | test/uat_e2e.sh |
| `SRG-APP-000125-WSR-000071` | V-206371 | MED | The log data and records from the web server must be backed up onto a different system or media. | The shipper moves web-tier records off the container filesystem into the PRIVATE ir-logs bucket, held apart from evidence and reachable only through the mesh; the store is backed up as a unit, so losing a container or its host no longer loses the log. Resumable by byte offsets kept beside the objects, from read-only source mounts. | backend/cases/management/commands/ship_logs.py, deploy/enclave/docker-compose.yml (log-shipper, read-only mounts) | test/uat_srg_webtier.sh — a deployed pass ships, objects and offset state are in the bucket, the bucket refuses unauthenticated reads, the sources are read-only |
| `SRG-APP-000131-WSR-000051` | V-206372 | MED | All web server files must be verified for their integrity (e.g., checksums and hashes) before becoming part of the production web server. | Every external FROM names a DIGEST, not a tag. A tag is a mutable pointer — alpine:3.24 is whatever the registry decides on the day of the build — so two builds of one source produced different images and neither could be said to be the one reviewed. Pulling by digest IS the integrity check: podman refuses content that does not hash to the name requested, so there is nothing further to verify at build time. ci/base-images.lock is the manifest record the control asks for. The Rust toolchain is RECORDED but not rewritten inline: build.sh passes RUST_VERSION from rust-toolchain.toml and an inline digest would silently win over that file. | ci/pin-base-images.sh (--check/--update/--apply), ci/base-images.lock, 16 FROM lines across backend, worker, collector, dmz, frontend, ingest, keycloak, workstation, re-workstation, consul sidecar, ntp, troubleshooting agent | uat_srg_webtier.sh — the check passes on the tree, and is proven to catch both failure modes by injection: a FROM reverted to a bare tag, and a FROM carrying a digest that disagrees with the lock |
| `SRG-APP-000131-WSR-000073` | V-206373 | MED | Expansion modules must be fully reviewed, tested, and signed before they can exist on a production web server. | There are no loadable expansion modules: the web tier runs fixed binaries from pinned images, with no plugin directory to populate. | frontend/Dockerfile, deploy/enclave/docker-compose.yml | **not verified** |
| `SRG-APP-000141-WSR-000015` | V-206374 | MED | The web server must not perform user management for hosted applications. | The web server performs no user management. Identity lives in Keycloak; accounts created in the admin UI are provisioned there, and the application holds no password. | backend/cases/keycloak_admin.py | test/uat_baseline.sh |
| `SRG-APP-000141-WSR-000075` | V-206375 | MED | The web server must only contain services and functions necessary for operation. | Each container runs one service from a minimal image; there is no general-purpose web server installation carrying optional features. | frontend/Dockerfile, deploy/enclave/docker-compose.yml | **not verified** |
| `SRG-APP-000141-WSR-000076` | V-206376 | MED | The web server must not be a proxy server. | nginx serves static content for one origin and proxies nothing outbound. Traefik is a REVERSE proxy for that single origin — the approved architecture — and the enclave has no egress for a forward proxy to use. | frontend/nginx.conf.template, traefik/dynamic-sso/dynamic.yml | test/uat_dns.sh — no service resolves an outside name |
| `SRG-APP-000141-WSR-000077` | V-206377 | MED | The web server must provide install options to exclude the installation of documentation, sample code, example applications, and tutorials. | The images are built from a Dockerfile that copies only the built application; no documentation, sample code or example content is installed. | frontend/Dockerfile | **not verified** |
| `SRG-APP-000141-WSR-000078` | V-206378 | MED | Web server accounts not utilized by installed features (i.e., tools, utilities, specific services, etc.) must not be created and must be deleted when the web server feature is uninstalled. | The images carry only the packages their service needs, so there are no accounts belonging to unused features to remove. | frontend/Dockerfile, backend/Dockerfile | **not verified** |
| `SRG-APP-000141-WSR-000080` | V-206379 | MED | The web server must provide install options to exclude installation of utility programs, services, plug-ins, and modules not necessary for operation. | Utility programs and unrelated services are absent from the images by construction — there is no installer offering them. | frontend/Dockerfile, backend/Dockerfile | **not verified** |
| `SRG-APP-000141-WSR-000081` | V-206380 | MED | The web server must have Multipurpose Internet Mail Extensions (MIME) that invoke OS shell programs disabled. | Served types are enumerated rather than inherited from the stock mime.types, and the default type is application/octet-stream, so an unknown extension downloads instead of being interpreted. | frontend/nginx.conf.template (types block, default_type) | test/uat_srg_webtier.sh — the explicit default type is asserted |
| `SRG-APP-000141-WSR-000082` | V-206381 | MED | The web server must allow the mappings to unused and vulnerable scripts to be removed. | There are no script mappings: nginx serves a static bundle and proxies API paths; no CGI, PHP or scripting handler is installed. | frontend/nginx.conf.template | **not verified** |
| `SRG-APP-000141-WSR-000083` | V-206382 | MED | The web server must have resource mappings set to disable the serving of certain file types. | Dotfiles and source maps are refused explicitly. None are in the image; the rule is what keeps that true after a build change, and a source map hands a reader the unminified application. | frontend/nginx.conf.template | test/uat_srg_webtier.sh — /.env and /assets/index.js.map both return 403 from the application server |
| `SRG-APP-000141-WSR-000085` | V-206383 | MED | The web server must have Web Distributed Authoring (WebDAV) disabled. | WebDAV is not compiled into or enabled in the nginx image, and no DAV methods are routed. | frontend/Dockerfile | **not verified** |
| `SRG-APP-000141-WSR-000086` | V-206384 | MED | The web server must protect system resources and privileged operations from hosted applications. | Hosted applications cannot reach system resources: containers are unprivileged, the worker drops all capabilities with a read-only root filesystem, and the data tier is reachable only through mesh sidecars that check an intention. | SECURITY-MODEL.md P7, P9 | test/uat_consul.sh; test/uat_e2e.sh |
| `SRG-APP-000141-WSR-000087` | V-206385 | MED | Users and scripts running on behalf of users must be contained to the document root or home directory tree of the web server. | nginx serves the built React bundle from its document root only; there is no CGI, no user home directories, and no path outside the image's static root. | frontend/nginx.conf.template, frontend/Dockerfile | **not verified** |
| `SRG-APP-000142-WSR-000089` | V-206386 | MED | The web server must be configured to use a specified IP address and port. | Every service binds a stated address and port; the data tier binds loopback only so its sidecar is the sole listener, and the ingress binds one TLS entrypoint. | SECURITY-MODEL.md P9; deploy/enclave/docker-compose.yml | test/uat_consul.sh |
| `SRG-APP-000172-WSR-000104` | V-206387 | HIG | The web server must encrypt passwords during transmission. | Credentials are entered only on the Keycloak login form, reached through the brokered session and served over TLS; the broker refuses a plaintext controller address outright. | SECURITY-MODEL.md P4; hashicorp/access/boundary_session.sh | test/uat_boundary.sh; test/uat_baseline.sh (browser OIDC login for all roles) |
| `SRG-APP-000176-WSR-000096` | V-206389 | MED | Only authenticated system administrators or the designated PKI Sponsor for the web server must have access to the web servers private key. | Private keys are readable only by the component that serves them. The DMZ holds the Boundary controller's certificate and NOT its key, which is asserted rather than assumed. | deploy/dmz/docker-compose.yml mounts boundary.crt only | test/uat_boundary.sh — the controller's private key is not readable in the DMZ |
| `SRG-APP-000206-WSR-000128` | V-206392 | MED | A web server utilizing mobile code must meet DoD-defined mobile code requirements. | A Content-Security-Policy constrains the React bundle to its own origin — default-src 'self', object-src 'none', frame-ancestors 'none' — applied to the application only, so the identity provider keeps the policy its login form needs. | traefik/dynamic-sso/dynamic.yml (app-csp middleware) | test/uat_srg_webtier.sh — the policy is present, defaults to 'self', and refuses framing; the IdP still serves its login endpoints |
| `SRG-APP-000211-WSR-000030` | V-206393 | MED | Web server accounts accessing the directory tree, the shell, or other operating system functions and utilities must only be administrative accounts. | Web-tier containers run as unprivileged users with no shell access granted to service accounts; the worker additionally drops all capabilities and runs read-only. | deploy/enclave/docker-compose.yml (worker hardening), frontend/Dockerfile | test/uat_re_workstation.sh (capability assertions on the RE session) |
| `SRG-APP-000211-WSR-000031` | V-206394 | MED | Anonymous user access to the web server application directories must be prohibited. | Anonymous access is impossible: oauth2-proxy fronts the application and redirects every unauthenticated request into the OIDC flow. | deploy/enclave/docker-compose.yml (--upstream=http://frontend:8080) | test/uat_baseline.sh |
| `SRG-APP-000211-WSR-000129` | V-206395 | MED | The web server must separate the hosted applications from hosted web server management functionality. | Management functions are separated from the hosted application: Keycloak's admin console and master realm are refused at the ingress, and management interfaces are reachable only through an administrator's own forwarder. | traefik/dynamic-sso/dynamic.yml (router idp-deny), admin/README.md | test/uat_baseline.sh — analysts receive 403 for /admin and /realms/master |
| `SRG-APP-000220-WSR-000201` | V-206396 | MED | The web server must invalidate session identifiers upon hosted application user logout or other session termination. | Sign-out ends the gate session AND the Keycloak session through the back-channel --backend- logout-url, so a returning browser must authenticate again rather than silently resuming. | deploy/enclave/docker-compose.yml (oauth2-proxy --backend-logout-url) | test/lib/oidc_logout.py — asserts re-entry lands on the login form and the KEYCLOAK_IDENTITY / KEYCLOAK_SESSION cookies are gone |
| `SRG-APP-000223-WSR-000011` | V-206397 | MED | Cookies exchanged between the web server and client, such as session cookies, must have security settings that disallow cookie access outside the originating web server and hosted application. | Secure, HttpOnly and SameSite=lax are all stated explicitly rather than inherited, with per- request CSRF cookies on a fifteen-minute expiry. | deploy/enclave/docker-compose.yml (oauth2-proxy cookie flags) | test/uat_srg_webtier.sh — the gate reports secure(https):true httponly:true samesite:lax |
| `SRG-APP-000223-WSR-000145` | V-206398 | MED | The web server must accept only system-generated session identifiers. | Session identifiers are minted by oauth2-proxy and Keycloak. Nothing in the application accepts a client-supplied identifier; the gate rejects a cookie it did not sign. | deploy/enclave/docker-compose.yml (oauth2-proxy --cookie-secret) | **not verified** |
| `SRG-APP-000224-WSR-000136` | V-206400 | MED | The web server must generate unique session identifiers that cannot be reliably reproduced. | Session identifiers come from Keycloak and oauth2-proxy's CSPRNG-backed session layer; neither derives an identifier from user or time input, so they are not reproducible. | keycloak/realm-irplatform.json, deploy/enclave/docker-compose.yml | **not verified** |
| `SRG-APP-000224-WSR-000137` | V-206401 | MED | The web server must generate a session ID long enough that it cannot be guessed through brute force. | Keycloak session identifiers are 128-bit UUIDs; the gate's cookie carries an AES-encrypted session payload keyed by --cookie-secret. Neither is brute-forceable. | keycloak/realm-irplatform.json | **not verified** |
| `SRG-APP-000224-WSR-000138` | V-206402 | MED | The web server must generate a session ID using as much of the character set as possible to reduce the risk of brute force. | Identifiers are base64/hex encodings over the full character set of their encoding, not a restricted alphabet. | keycloak/realm-irplatform.json | **not verified** |
| `SRG-APP-000224-WSR-000139` | V-206403 | MED | The web server must generate unique session identifiers with definable entropy. | Entropy is a property of the identity provider's session layer (128-bit), not a tunable of the deployment. | keycloak/realm-irplatform.json | **not verified** |
| `SRG-APP-000225-WSR-000074` | V-206404 | MED | The web server must augment re-creation to a stable and known baseline. | Re-creation to a known baseline is the normal path: every container is rebuilt from source by deploy.sh, which also refuses to run a container whose image predates the code. | deploy/deploy.sh (verify_image, recreate_if_stale) | test/uat_e2e.sh (the deployment is rebuilt and re-validated end to end) |
| `SRG-APP-000225-WSR-000140` | V-206405 | MED | The web server must be built to fail to a known safe state if system initialization fails, shutdown fails, or aborts fail. | Failure is to a closed state: the broker refuses to open a listener without a session, Vault comes back sealed, the receiver refuses to start without a certificate, and each deploy stage gates on a health check rather than proceeding. | hashicorp/access/boundary_session.sh, deploy/deploy.sh | test/uat_boundary.sh |
| `SRG-APP-000233-WSR-000146` | V-206408 | MED | The web server document directory must be in a separate partition from the web servers system files. | Document root and system files are separated by construction: the served bundle lives in the image, evidence lives in object storage, and writable state is a named volume rather than a directory under the document root. | frontend/Dockerfile, deploy/enclave/docker-compose.yml | **not verified** |
| `SRG-APP-000246-WSR-000149` | V-206409 | MED | The web server must restrict the ability of users to launch Denial of Service (DoS) attacks against other information systems or networks. | A request-rate limit on the ingress. Every analyst arrives from the egress worker, so the source is shared and this bounds the platform's total rate — which is the denial-of-service property that matters here. | traefik/dynamic-sso/dynamic.yml (ratelimit middleware) | test/uat_srg_webtier.sh — the rate limit is asserted as deployed |
| `SRG-APP-000251-WSR-000157` | V-206410 | MED | The web server must limit the character set used for data entry. | Input is validated and typed by the API serializers, and the ORM parameterizes every query; the character set accepted is defined by the field type. | backend/cases/serializers.py | **not verified** |
| `SRG-APP-000251-WSR-000194` | V-264364 | MED | The web server must interpret and normalize ambiguous HTTP requests or terminate the TCP connection. | A request carrying both Content-Length and Transfer-Encoding is normalized to a single request by the ingress rather than resolved inconsistently. The control permits normalizing or terminating; this normalizes, and the smuggled second request is never served. | traefik/traefik.yml — the ingress runtime enforces RFC 7230 framing | test/uat_srg_webtier.sh — a crafted desync request produces exactly ONE response; the assertion counts responses rather than status codes, because a desync shows up as a second response, not an error |
| `SRG-APP-000251-WSR-000195` | V-264365 | MED | The web server must terminate the connection if server-level exceptions are triggered when handling requests to prevent HTTP request smuggling attacks. | The ambiguity is resolved before dispatch, so the smuggled request is discarded rather than passed upstream as a request of its own — the ingress and its upstream cannot disagree about where one request ends. | traefik/traefik.yml | test/uat_srg_webtier.sh — no second response is produced |
| `SRG-APP-000266-WSR-000142` | V-206411 | MED | The web server must display a default hosted application web page, not a directory listing, when a requested web page cannot be found. | autoindex is off, and the SPA fallback means a miss lands on the application rather than on an index of the document root. | frontend/nginx.conf.template | test/uat_srg_webtier.sh — asserted in the rendered configuration |
| `SRG-APP-000266-WSR-000159` | V-206412 | MED | Warning and error messages displayed to clients must be modified to minimize the identity of the web server, patches, loaded modules, and directory paths. | The ingress strips Server and X-Powered-By on the way out (an empty value removes the header) and nginx sets server_tokens off, so an error served before the ingress sees it does not name the server either. | traefik/dynamic-sso/dynamic.yml (sec-headers), frontend/nginx.conf.template | test/uat_srg_webtier.sh — no Server or X-Powered-By header; HSTS, nosniff, frame-deny and referrer-policy present |
| `SRG-APP-000266-WSR-000160` | V-206413 | MED | Debugging and trace information used to diagnose the web server must be disabled. | Django runs with DEBUG disabled, so tracebacks and settings are never rendered to a client; Traefik's debug endpoints are not exposed. | deploy/.env DJANGO_DEBUG=0; confirmed on the running backend (settings.DEBUG is False) | **not verified** |
| `SRG-APP-000295-WSR-000012` | V-206414 | MED | The web server must set an absolute session timeout value of eight hours or less. | An eight-hour absolute session lifetime, stated in both halves: the gate's cookie expiry and the realm's ssoSessionMaxLifespan. The shorter wins, and setting only one leaves the other's default in force. | deploy/enclave/docker-compose.yml (--cookie-expire=8h), hashicorp/keycloak/realm- irplatform.json (ssoSessionMaxLifespan) | test/uat_srg_webtier.sh — the gate reports expiry:8h0m0s at startup, which is the deployed value |
| `SRG-APP-000295-WSR-000134` | V-206415 | MED | The web server must set an inactive timeout for sessions. | The session is re-validated against the identity provider every fifteen minutes, which is what makes an idle session lapse and a revoked account stop working mid-session, paired with the realm's ssoSessionIdleTimeout. | deploy/enclave/docker-compose.yml (--cookie-refresh=15m), hashicorp/keycloak/realm- irplatform.json (ssoSessionIdleTimeout) | test/uat_srg_webtier.sh — the gate reports refresh:after 15m0s |
| `SRG-APP-000315-WSR-000003` | V-206416 | MED | Remote access to the web server must follow access policy or work in conjunction with enterprise tools designed to enforce policy requirements. | Remote access is a Boundary session against one target, from a workstation enrolled on a deny-by-default tailnet. There is no second path to permit or forbid. | SECURITY-MODEL.md P1, P3 | test/uat_boundary.sh; test/uat_tailnet.sh — the tunnel carries the session and bounds it (no internal reach) |
| `SRG-APP-000315-WSR-000004` | V-206417 | MED | The web server must restrict inbound connections from nonsecure zones. | The enclave accepts no connection originated in the DMZ except the Boundary controller API and the egress worker's proxy port, both authenticated. The bastion cannot reach the ingress directly. | SECURITY-MODEL.md P1 | test/uat_boundary.sh — the bastion cannot reach the enclave ingress except through a session |
| `SRG-APP-000316-WSR-000170` | V-206418 | MED | The web server must provide the capability to immediately disconnect or disable remote access to the hosted applications. | Canceling a Boundary session immediately drops the analyst's connection, and the broker is supervised so it re-establishes only as a new authorized session. | hashicorp/access/boundary_session.sh | test/uat_boundary.sh §5a — a canceled session is replaced by a NEW authorized one, unattended |
| `SRG-APP-000334-WSR-000024` | V-279028 | MED | The web server must uniquely identify and authenticate source by organization, system, application, and/or individual for information transfer. | Service-to-service identity is a Connect certificate checked against an intention, not an address; user identity is an OIDC assertion. Both name the source explicitly. | SECURITY-MODEL.md P9 | test/uat_consul.sh — denied pairs are refused on the wire |
| `SRG-APP-000340-WSR-000029` | V-206419 | MED | Non-privileged accounts on the hosting system must only access web server security-relevant information and functions through a distinct administrative account. | Security-relevant views (audit trail, platform health, mesh, sessions, repairs) are gated on the auditor and admin roles; analysts do not see them. | backend/cases/rbac.py, backend/cases/views.py | test/uat_e2e.sh — RBAC and audit sections |
| `SRG-APP-000357-WSR-000150` | V-206421 | MED | The web server must use a logging mechanism that is configured to allocate log record storage capacity large enough to accommodate the logging requirements of the web server. | Log record storage carries a declared allocation (IR_LOGS_ALLOC_BYTES) and every health report measures bucket consumption against it, so capacity is a stated figure rather than whatever the volume happens to hold. | backend/cases/management/commands/ship_logs.py (ALLOC_BYTES, log_storage), backend/cases/componenthealth.py (_log_storage_alerts) | test/uat_srg_webtier.sh — the allocation is declared and reported with measured usage |
| `SRG-APP-000358-WSR-000063` | V-206422 | MED | The web server must not impede the ability to write specified log record content to an audit log server. | Nothing filters or truncates log content: the audit ledger stores full entries and container logs are unmodified. | backend/cases/audit.py | **not verified** |
| `SRG-APP-000358-WSR-000163` | V-206423 | MED | The web server must be configurable to integrate with an organizations security infrastructure. | Integration is a read against the ir-logs bucket: shipped objects are whole structured records a SIEM consumes as-is, and the enclave never dials out to deliver them. | backend/cases/management/commands/ship_logs.py (whole-line objects), traefik/traefik.yml (JSON access log) | test/uat_srg_webtier.sh — a shipped object is fetched and parses as structured records |
| `SRG-APP-000359-WSR-000065` | V-206424 | MED | The web server must use a logging mechanism that is configured to provide a warning to the ISSO and SA when allocated record storage volume reaches 75% of maximum log record storage capacity. | Component Health warns at 75% of the declared log storage allocation and goes critical at 95%, on the same page and alert channel every other capacity figure uses. | backend/cases/componenthealth.py (LOG_STORAGE_PERCENT_WARN, _log_storage_alerts) | test/uat_srg_webtier.sh — 76% of allocation warns and 50% does not, in the deployed code |
| `SRG-APP-000374-WSR-000172` | V-206425 | MED | The web server must generate log records that can be mapped to Coordinated Universal Time (UTC) or Greenwich Mean Time (GMT). | Timestamps are recorded in UTC throughout, and the UI labels the zone on every rendered time. | backend/cases/audit.py; UI_OVERVIEW.md (display preferences) | test/uat_ui.sh |
| `SRG-APP-000375-WSR-000171` | V-206426 | MED | The web server must record time stamps for log records to a minimum granularity of one second. | Timestamps carry sub-second precision, which exceeds the one-second minimum. | backend/cases/audit.py | **not verified** |
| `SRG-APP-000380-WSR-000072` | V-206427 | MED | The web server application, libraries, and configuration files must only be accessible to privileged users. | Configuration reaches containers as read-only mounts, secrets come from Vault Agent rather than files in the image, and the runtime socket is absent from every web-tier container. | SECURITY-MODEL.md P2, P10; deploy/enclave/docker-compose.yml | test/uat_repairs.sh — the executor's posture is asserted from the running container |
| `SRG-APP-000383-WSR-000175` | V-206428 | MED | The web server must prohibit or restrict the use of nonsecure or unnecessary ports, protocols, modules, and/or services. | Cleartext and unnecessary ports are disabled rather than firewalled: Consul's HTTP and gRPC cleartext ports are off, ops listeners bind loopback, and the networks are internal with no egress. | hashicorp/consul/server.hcl (http = -1); SECURITY-MODEL.md P4, P6 | test/uat_consul.sh — cleartext ports closed; test/uat_dns.sh |
| `SRG-APP-000429-WSR-000113` | V-206431 | HIG | The web server must encrypt user identifiers and passwords. | Identifiers and passwords cross the wire only under TLS, and Keycloak stores passwords hashed — the application holds no password of its own. | SECURITY-MODEL.md P4; keycloak/realm-irplatform.json | test/uat_baseline.sh |
| `SRG-APP-000435-WSR-000147` | V-206432 | MED | The web server must be protected from being stopped by a non-privileged user. | The web tier holds no container runtime access, so no process it runs can stop the server; stopping is an operator action on the host, or a named repair request the isolated agent executes. | SECURITY-MODEL.md P10; troubleshooting/remediation-agent.sh | test/uat_repairs.sh — unknown actions and non-admin principals are refused |
| `SRG-APP-000435-WSR-000148` | V-206433 | MED | The web server must be tuned to handle the operational requirements of the hosted application. | Tuning is derived from the hardware each container actually has rather than fixed: Postgres settings come from pg-tune.sh, and the worker's concurrency and memory limits are declared. | hashicorp/postgres/pg-tune.sh, deploy/enclave/docker-compose.yml | **not verified** |
| `SRG-APP-000439-WSR-000151` | V-206434 | HIG | The web server must employ cryptographic mechanisms (TLS/DTLS/SSL) preventing the unauthorized disclosure of information during transmission. | TLS on the ingress, Boundary per-session mutual TLS on the approach, Connect mutual TLS between enclave services. No hop in the analyst path is cleartext. | SECURITY-MODEL.md P4 and P9 | test/uat_boundary.sh; test/uat_consul.sh |
| `SRG-APP-000439-WSR-000152` | V-206435 | MED | Web server session IDs must be sent to the client using SSL/TLS. | Session identifiers travel only inside the TLS session; the gate is not reachable over cleartext. | traefik/traefik.yml (TLS entrypoint) | test/uat_baseline.sh |
| `SRG-APP-000439-WSR-000153` | V-206436 | MED | Web server cookies, such as session cookies, sent to the client using SSL/TLS must not be compressed. | The session cookie is not compressed, and cannot be: the gate offers no cookie compression option, and with the Redis session store the cookie carries a REFERENCE rather than session content — there is nothing in it whose compressed length could leak. | deploy/enclave/docker-compose.yml (--session-store-type=redis); oauth2-proxy exposes no cookie-compression flag | test/uat_srg_webtier.sh — the server-side session store is asserted, which is what makes the cookie a ticket |
| `SRG-APP-000439-WSR-000154` | V-206437 | MED | Cookies exchanged between the web server and the client, such as session cookies, must have cookie properties set to prohibit client-side scripts from reading the cookie data. | The gate sets --cookie-secure=true, so the session cookie is sent only over TLS. | deploy/enclave/docker-compose.yml (oauth2-proxy --cookie-secure=true) | **not verified** |
| `SRG-APP-000439-WSR-000155` | V-206438 | MED | Cookies exchanged between the web server and the client, such as session cookies, must have cookie properties set to force the encryption of cookies. | HttpOnly is set explicitly, so the flag does not rest on an image default that can change. | deploy/enclave/docker-compose.yml (--cookie-httponly=true) | test/uat_srg_webtier.sh — the gate reports httponly:true |
| `SRG-APP-000439-WSR-000156` | V-206439 | MED | A web server must maintain the confidentiality of controlled information during transmission through the use of an approved TLS version. | All transmission to and from the web tier is TLS-protected, and the enclave has no egress path over which anything could leave unprotected. | SECURITY-MODEL.md P4, P6 | test/uat_dns.sh; test/uat_boundary.sh |
| `SRG-APP-000439-WSR-000188` | V-206440 | MED | The web server must remove all export ciphers to protect the confidentiality and integrity of transmitted information. | Export, NULL, RC4 and DES suites are absent because the cipher list names only the six suites that are permitted. | traefik/dynamic-sso/dynamic.yml (tls.options.default.cipherSuites) | test/uat_srg_webtier.sh — a client offering only export/NULL/RC4/DES is refused |
| `SRG-APP-000439-WSR-000192` | V-264362 | MED | The web server must use HTTP/2, at a minimum. | HTTP/2 is configured on the TLS entrypoint and selected over ALPN. | traefik/traefik.yml (entryPoints.websecure.http2) | test/uat_srg_webtier.sh — ALPN selects h2 |
| `SRG-APP-000439-WSR-000196` | V-264366 | MED | The web server must only use forward proxies that route HTTP/2 requests upstream. | The platform runs no forward proxy. Traefik is a reverse proxy for one origin, and nothing in the enclave has an outbound route for a forward proxy to serve. | SECURITY-MODEL.md P6; deploy/enclave/docker-compose.yml | test/uat_dns.sh |
| `SRG-APP-000441-WSR-000181` | V-206441 | MED | The web server must maintain the confidentiality and integrity of information during preparation for transmission. | Information is prepared for transmission inside the TLS session; the mesh carries service- to-service traffic under mutual TLS. | SECURITY-MODEL.md P4, P9 | test/uat_consul.sh |
| `SRG-APP-000442-WSR-000182` | V-206442 | MED | The web server must maintain the confidentiality and integrity of information during reception. | Reception is the same path in reverse — the ingress accepts only TLS, and data-tier listeners are loopback-bound behind their sidecars. | SECURITY-MODEL.md P9 | test/uat_consul.sh — denied pairs are refused on the wire |
| `SRG-APP-000456-WSR-000187` | V-206443 | MED | The web server must install security-relevant software updates within 30 days unless the time period is directed by an authoritative source (e.g., IAVM, CTOs, DTMs, STIGs). | ci/image-currency.sh already asked the registry whether each pin had moved on; detecting drift was never the gap. The gap was that nothing recorded WHEN a review happened, so 'we check regularly' was unfalsifiable. --record writes the date AND the finding count, because a review that found five stale images and one that found none are different events; --age fails once the record passes the 30-day ceiling the control names. | ci/image-currency.sh (--record, --age), ci/image-currency.record | uat_srg_webtier.sh — the record exists, is within the interval, and the gate is proven to fail on an overdue record by backdating one |
| `SRG-APP-000516-WSR-000079` | V-206444 | MED | All accounts installed with the web server software and tools must have passwords assigned and default passwords changed. | No default or shared web-server accounts exist. Service identities are container users with no password and no shell; human accounts live only in Keycloak. | frontend/Dockerfile, backend/Dockerfile | **not verified** |
| `SRG-APP-000815-WSR-000160` | V-264342 | MED | The web server must require users to be individually authenticated before granting access to the shared accounts or resources. | Each analyst authenticates individually to Keycloak before reaching the application, and every mutation is attributed to that account in the audit trail. | backend/cases/authentication.py, backend/cases/audit.py | test/uat_e2e.sh — audit entries carry the acting principal |
| `SRG-APP-000830-WSR-000190` | V-264345 | MED | The web server must, for password-based authentication, maintain a list of commonly used, expected, or compromised passwords on an organization-defined frequency. | The realm enforces a password policy: length 15, all four character classes, notUsername and notEmail, history 5, forced change on expiry. Read from the RUNNING realm, because an import never overwrites an existing one — deploy.sh recreates Keycloak when the realm file changes so the two cannot disagree. notUsername and notEmail refuse the two guesses made first. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) | test/uat_srg_webtier.sh — the policy is read back from the running realm and each element asserted |
| `SRG-APP-000835-WSR-000200` | V-264346 | MED | The web server must, for password-based authentication, update the list of passwords on an organization-defined frequency. | Keycloak passwordBlacklist(dfir-platform.txt), added to the realm password policy that converges on every deploy (D-008). The list is baked into the image beside the theme and for the same reason: a policy naming a file Keycloak cannot read fails password SETTING, and the first sign would be an analyst unable to complete a forced reset. Seeded with platform CONTEXT terms as well as weak forms — 'IrPlatform#2026' satisfies length, case, digits and specials, and is the first thing an attacker who has read the public repository would try. A composition rule cannot catch that; a list can. It gates password setting only, never sign-in. | keycloak/password-blacklists/dfir-platform.txt, keycloak/Dockerfile (COPY + build-time test -s), hashicorp/keycloak/realm-irplatform.json passwordPolicy | uat_srg_webtier.sh — the policy names the list, the file is present and non-empty inside the running Keycloak, and the review record is within its interval |
| `SRG-APP-000840-WSR-000210` | V-264347 | MED | The web server must, for password-based authentication, update the list of passwords when organizational passwords are suspected to have been compromised directly or indirectly. | The same list and the same converge path. This control differs from WSR-000200 only in its trigger — suspected compromise rather than a schedule — so the mechanism is shared and the RECORD is what distinguishes them: --record takes a reason, and an out-of-cycle update states what prompted it. | keycloak/password-blacklists/dfir-platform.txt, ci/password-blacklist-check.sh | uat_srg_webtier.sh — same assertions as WSR-000200, plus the reason field on the record |
| `SRG-APP-000845-WSR-000220` | V-264348 | MED | The web server must, for password-based authentication, verify when users create or update passwords, that the passwords are not found on the list of commonly-used, expected, or compromised passwords in IA-5 (1) (a). | The realm enforces a password policy: length 15, all four character classes, notUsername and notEmail, history 5, forced change on expiry. Read from the RUNNING realm, because an import never overwrites an existing one — deploy.sh recreates Keycloak when the realm file changes so the two cannot disagree. Verification happens on every set, since the policy is the realm's. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) | test/uat_srg_webtier.sh — the policy is read back from the running realm and each element asserted |
| `SRG-APP-000850-WSR-000230` | V-264349 | MED | The web server must, for password-based authentication, store passwords using an approved salted key derivation function, preferably using a keyed hash. | Passwords are stored salted and hashed with pbkdf2-sha512 at 210,000 iterations, stated explicitly in the realm rather than inherited from an image default that drifts between Keycloak releases. The platform never sees or stores a password. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) — hashAlgorithm(pbkdf2-sha512) and hashIterations(210000) | test/uat_srg_webtier.sh — an approved salted hash is asserted on the running realm |
| `SRG-APP-000855-WSR-000240` | V-264350 | MED | The web server must, for password-based authentication, require immediate selection of a new password upon account recovery. | New accounts are created with a temporary password and Keycloak requires a change at first sign-in. | backend/cases/keycloak_admin.py (temporary password + required action) | test/uat_ui.sh (user creation) |
| `SRG-APP-000860-WSR-000250` | V-264351 | MED | The web server must, for password-based authentication, allow user selection of long passwords and passphrases, including spaces and all printable characters. | The realm enforces a password policy: length 15, all four character classes, notUsername and notEmail, history 5, forced change on expiry. Read from the RUNNING realm, because an import never overwrites an existing one — deploy.sh recreates Keycloak when the realm file changes so the two cannot disagree. A 15-character minimum, with no upper bound — long passwords are permitted. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) | test/uat_srg_webtier.sh — the policy is read back from the running realm and each element asserted |
| `SRG-APP-000865-WSR-000260` | V-264352 | MED | The web server must, for password-based authentication, employ automated tools to assist the user in selecting strong password authenticators. | The realm enforces a password policy: length 15, all four character classes, notUsername and notEmail, history 5, forced change on expiry. Read from the RUNNING realm, because an import never overwrites an existing one — deploy.sh recreates Keycloak when the realm file changes so the two cannot disagree. Keycloak's login form reports the policy as the user types, once one exists. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) | test/uat_srg_webtier.sh — the policy is read back from the running realm and each element asserted |
| `SRG-APP-000870-WSR-000270` | V-264353 | MED | The web server must, for password-based authentication, enforce organization-defined composition and complexity rules. | The realm enforces a password policy: length 15, all four character classes, notUsername and notEmail, history 5, forced change on expiry. Read from the RUNNING realm, because an import never overwrites an existing one — deploy.sh recreates Keycloak when the realm file changes so the two cannot disagree. Composition rules and reuse history are stated in the policy. | hashicorp/keycloak/realm-irplatform.json (passwordPolicy) | test/uat_srg_webtier.sh — the policy is read back from the running realm and each element asserted |
| `SRG-APP-000880-WSR-000290` | V-264355 | MED | The web server must protect nonlocal maintenance sessions by separating the maintenance session from other network sessions with the system by logically separated communications paths. | Maintenance access is separated from user access by construction: administrators reach management interfaces through their own forwarder, not through the analyst origin, and the analyst target reaches only the SSO gate. | admin/README.md; SECURITY-MODEL.md P3 | test/uat_boundary.sh — exactly one target exists, and it is the SSO gate |
| `SRG-APP-000910-WSR-000300` | V-264356 | MED | The web server must include only approved trust anchors in trust stores or certificate stores managed by the organization. | Trust is pinned, not inherited: each client trusts exactly the CA it must. tailscaled's SSL_CERT_FILE REPLACES the system trust pool with the control-plane certificate, and Boundary clients pin BOUNDARY_CACERT. | deploy/dmz/docker-compose.yml (SSL_CERT_FILE), hashicorp/access/boundary_session.sh | test/uat_boundary.sh — the certificate is pinned by the client |
| `SRG-APP-000915-WSR-000310` | V-264357 | MED | The web server must provide protected storage for cryptographic keys with organization-defined safeguards and/or hardware protected key store. | Vault holds key material and app secrets, with dynamic database credentials and an audit device; the unseal key is the operator's, not a container's. The custody HMAC key comes from KV rather than a file. | SECURITY-MODEL.md P2; hashicorp/vault/vault-setup-ir.sh | test/uat_vault.sh — credentials are dynamic, minted live, and revocation works |
| `SRG-APP-000920-WSR-000320` | V-264358 | MED | The web server must synchronize system clocks within and between systems or system components. | The enclave runs its own chrony, staged beside the resolver and for the same reason: `internal: true` installs no route off the host, so both authorities must live inside the segment. It serves the enclave HOSTS, whose chrony clients discipline their own clocks; containers inherit from the host and cannot set time, so the service runs with -x and never touches a clock it does not own. This matters more here than on an ordinary web tier: temporal_coherence compares instants ACROSS hosts, so a drifting enclave does not produce a wrong-looking record, it places a plausible one at the wrong point on the timeline. | ntp/Dockerfile, ntp/chrony.conf, ntp/entrypoint.sh, deploy/enclave/docker-compose.yml (ntp on internal + dmzlink), deploy/deploy.sh stage 0a, ci/clock-sync-check.sh | uat_srg_webtier.sh — the service answers chronyc and is serving; the host clock is synchronized; and a container's clock is proven to agree with the host's, which is the inheritance the design assumed and had never tested |
| `SRG-APP-000925-WSR-000330` | V-264359 | MED | The web server must compare the internal system clocks on an organization-defined frequency with organization-defined authoritative time source. | Comparison against an authoritative source, with the air gap stated rather than hidden. IR_NTP_UPSTREAM names the reference and the service is disciplined by it; unset, the service serves from a LOCAL stratum 10 reference and the check reports the segment as internally consistent but NOT traceable. Stratum 10 is deliberately poor so any client that can reach a real source prefers it. CONDITIONAL BY DESIGN: a deployment that names no upstream does not satisfy traceability, and the check says so instead of passing quietly — a check that failed on the designed air-gapped case would simply be turned off. | ntp/entrypoint.sh (upstream templating), ci/clock-sync-check.sh, deploy/.env IR_NTP_UPSTREAM | uat_srg_webtier.sh — when IR_NTP_UPSTREAM is set the upstream must actually be reached, so an unreachable server cannot read the same as a deliberate air gap; when unset the untraceable state is reported explicitly |
| `SRG-APP-001035-WSR-000340` | V-279029 | HIG | The web server must be a version supported by the vendor. | Every component runs a current vendor-supported release, and drift is detected rather than assumed — the currency check compares pinned versions against upstream stable. | ci/image-currency.sh; deploy/enclave/docker-compose.yml (pinned versions) | ci/image-currency.sh |

